{
  "name": "bastion",
  "version": "1.0.0",
  "description": "Passive website security scanner for small businesses, built on Cloudflare Workers.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
